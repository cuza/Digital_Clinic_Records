    {% extends ':default:backend.html.twig' %}

    {% block BackendContent %}
    <h1>User</h1>

    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ user.nombre }}</td>
            </tr>
            <tr>
                <th>Cid</th>
                <td>{{ user.cId }}</td>
            </tr>
        </tbody>
    </table>


{{ form_start(delete_form) }}
<div class="btn-group" role="group" aria-label="...">
	
<a role="button" class="btn btn-default" data-toggle="tooltip" data-placement="top" title="Atras" href="{{ path('user_index') }}"><i class="fa fa-chevron-left"></i></a>
<a role="button" class="btn btn-default" data-toggle="tooltip" data-placement="top" title="Editar" href="{{ path('user_edit', { 'id': user.id }) }}"><i class="fa fa-edit"></i></a>
<button type="submit" class="btn btn-default" data-toggle="tooltip" data-placement="top" title="Eliminar" ><i class="fa fa-trash"></i></button>
</div>
{{ form_end(delete_form) }}{% endblock %}
