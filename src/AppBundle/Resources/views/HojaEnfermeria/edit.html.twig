    {% extends '::base.html.twig' %}

    {% block ContentBody %}
    <h1>HojaEnfermeria edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}


{{ form_start(delete_form) }}
<div class="btn-group" role="group" aria-label="...">
	
<a role="button" class="btn btn-default" data-toggle="tooltip" data-placement="top" title="Atras" href="{{ path('hojaenfermeria_index') }}"><i class="fa fa-chevron-left"></i></a>
<button type="submit" class="btn btn-default" data-toggle="tooltip" data-placement="top" title="Eliminar" ><i class="fa fa-trash"></i></button>
</div>
{{ form_end(delete_form) }}{% endblock %}
